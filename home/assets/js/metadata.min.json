---
# customized
# metadata for table ajax
---
{%- assign items = site.data[site.metadata] | where_exp: 'item','item.objectid and item.search_index and item.data_location' -%}
{%- assign fields = site.data.config-table | map: 'field' -%}
{ "objects": [
{%- for item in items -%}[ {{ item.title | jsonify }}, {{ item.item_count | jsonify }}, {{ item.description | jsonify }}, {{ item.categories | jsonify }}, "{{ item.data_location }}", "{{ item.url }}" ]{% unless forloop.last %},{% endunless %}
{% endfor %}
] }
